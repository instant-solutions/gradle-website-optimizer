subprojects {
    apply plugin: 'java'
    apply plugin: 'maven-publish'

    repositories {
        mavenCentral()
    }

    ext {
        currentVersion = "0.1-SNAPSHOT"
    }
}


task wrapper(type: Wrapper) {
    gradleVersion = '2.4'
}
